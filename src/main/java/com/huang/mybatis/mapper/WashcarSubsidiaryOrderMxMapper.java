package com.huang.mybatis.mapper;

import com.huang.mybatis.pojo.WashcarSubsidiaryOrderMx;

public interface WashcarSubsidiaryOrderMxMapper {
    int insert(WashcarSubsidiaryOrderMx record);

    int insertSelective(WashcarSubsidiaryOrderMx record);
}